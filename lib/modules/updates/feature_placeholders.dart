import 'package:flutter/material.dart';

// Pronunciation Correction Placeholder
class PronunciationCorrectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Pronunciation")),
      body: const Center(child: Text("This feature will be available in Module 2.")),
    );
  }
}

// Game Placeholder
class GameScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Game")),
      body: const Center(child: Text("This feature will be available in Module 2.")),
    );
  }
}

// Quiz Placeholder
class QuizScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Quiz")),
      body: const Center(child: Text("This feature will be available in Module 2.")),
    );
  }
}
